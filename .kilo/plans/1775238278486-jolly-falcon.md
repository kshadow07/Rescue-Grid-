# Plan: Phase 6 — Task Force Management

## Current State
- Phase 5 (Assignment System) is **COMPLETE**
- Phase 6 (Task Force Management) is **PENDING**

## Phase 6 Tasks

### 6.1 Deployments Page (`/dma/deployments`)
- [ ] Create `app/(dma)/dma/deployments/page.tsx`
- [ ] Card list: all task forces (active + dissolved)
- [ ] Each card: TF name, member count, status badge, linked assignment name
- [ ] `OPEN ROOM` secondary Button → navigate to `/dma/messages?tf=[id]`
- [ ] `DISSOLVE` danger Button → PATCH `/api/dma/taskforce/[id]` `{ status: 'dissolved' }`
- [ ] `+ CREATE TASK FORCE` primary Button → opens Create TF modal

### 6.2 Create Task Force Modal
- [ ] Create `components/dma/CreateTaskForceModal.tsx`
- [ ] `NAME` InputField (required)
- [ ] `ADD MEMBERS` — multi-select from `volunteer` rows: show name + type + status badge
- [ ] `ASSIGN MISSION (optional)` — dropdown of active assignments
- [ ] `CREATE` primary Button → POST `/api/dma/taskforce`
- [ ] `CANCEL` ghost Button → closes modal

### 6.3 API Routes — Task Force

#### `POST /api/dma/taskforce`
- [ ] Insert into `task_force`
- [ ] Insert one `task_force_member` row per selected volunteer
- [ ] Return created TF with members

#### `PATCH /api/dma/taskforce/[id]`
- [ ] Update `task_force.status` (dissolve)
- [ ] Support member add: `{ addVolunteer: uuid }` → insert `task_force_member`
- [ ] Support member remove: `{ removeVolunteer: uuid }` → delete `task_force_member`

### 6.4 Supporting API Routes
- [ ] `GET /api/dma/taskforce/list` — list all task forces with member count and assignment info

## Implementation Order
1. API route: `POST /api/dma/taskforce`
2. API route: `PATCH /api/dma/taskforce/[id]`
3. API route: `GET /api/dma/taskforce/list`
4. CreateTaskForceModal component
5. Deployments page
6. Update BUILD_PROGRESS.md

## Verification Steps
1. Navigate to `/dma/deployments` after login
2. Click `+ CREATE TASK FORCE` → modal opens
3. Enter name, select 2+ volunteers, optionally assign mission
4. Click CREATE → TF appears in list
5. Click DISSOLVE → TF status changes to dissolved
6. Click OPEN ROOM → navigates to messages page
